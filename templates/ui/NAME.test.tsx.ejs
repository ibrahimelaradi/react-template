import React from "react";
import { render, screen } from "@testing-library/react";
import { <%= name.pascal %>  } from "./<%= name.kebab %>";

test("Write your own test!", () => {
  // Write <%= name.pascal %> tests
});
